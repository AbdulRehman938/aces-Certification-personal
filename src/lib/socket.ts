"use client";
export type SocketInstance = unknown;

export const connectSocket = (): SocketInstance | null => null;
export const disconnectSocket = () => {};
export const getSocket = (): SocketInstance | null => null;
